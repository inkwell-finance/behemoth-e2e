{
  "name": "@inkwell-finance/behemoth-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for Behemoth multi-repo system",
  "type": "module",
  "scripts": {
    "test": "bun test --timeout 120000",
    "test:watch": "bun test --watch",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.9.3"
  }
}
